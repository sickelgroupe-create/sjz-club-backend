# 初始化验证记录

## 2026-09-20 剩余项补修复核

使用更新后的交付工具 `deploy/tools/init_database.py` 向新建隔离库导入001/002成功；按表名与当前基线逐项比对75表，而非固定旧表数。新增 `club_aftersale_admin_request` 保存审核/重开请求历史。旧隔离库成功执行售后历史表迁移及钱包游标索引迁移，后者连跑两次通过。原业务数据未删除；没有对生产执行。

完整应用实库469项请求/权限检查通过，含同case并发重开200/200只流转一次、多轮旧key防重放、余额批准退款只一条账变；钱包5页138条、翻页中新流水、本人隔离及非法参数测试通过。新预检工具在隔离MySQL执行了表/字段/索引及历史异常查询，正确识别空库管理员尚未激活。

以下74表记录属于补修前快照，当前交付为75表。真实客户、资金、微信/Apple记录及新服务器均未验收。

## 2026-09-20 当前结构复核

在新建的独立 MySQL 8.4.11 实例中，将当前 001、002 导入另一个空库成功：74 表、85 菜单、175 角色菜单关系；客户、订单、身份信息均为零，初始管理员仍禁用。旧版本测试库已成功应用菜单、订单过期和原生微信退款重试迁移；菜单迁移重复执行两次成功。均未连接生产数据库。

另在 MySQL 临时表复现：旧追偿 SQL 在100元欠款只回收50元时错误标为 recovered；修正后50元为 outstanding、100元才为 recovered。金额0.005也可复现两个DECIMAL列分开舍入造成余额/冻结不一致，接口现在拒绝不足一分或非整分金额。

以上是结构及隔离测试证据，不代表历史生产数据已核账，也不等于真实支付渠道验收。

## 历史记录

日期：2026-09-16。环境：本机临时隔离 MySQL 8.4.11，未连接生产数据库。

- 按 migration-order.json 所列来源建立最终结构。
- 将最终结构与配置导出后，重新导入新的空数据库：74 张表、62 个菜单节点、171 条角色菜单关联。
- 用户、订单、支付、钱包和身份资料表均为 0 行。
- 管理员初始禁用，密码为不可登录占位值；无公开可用初始密码。
- Python 导入工具在另一个空数据库成功执行，验证了 74 张表。
- 管理员设置工具生成临时随机密码的 BCrypt 2a 散列，激活测试库管理员成功且密码校验通过；测试凭据不在本目录。

此记录不等于三端应用启动、现网迁移、支付或退款的验收。

2026-09-19 增补：001 中的 club_virtual_payment 增加 payment_checked_at、refund_checked_at 及配套索引；旧库升级脚本为 migrations/20260919_virtual_payment_reconciliation.sql，表数仍为 74。新增支付业务回归在隔离 H2 MySQL 模式执行，含事务回滚和并发冲正验证；此轮没有重新在 MySQL 运行完整导入，也未升级任何生产数据库。不要把 09-16 的导入记录当作新迁移已经在线执行的证据。
