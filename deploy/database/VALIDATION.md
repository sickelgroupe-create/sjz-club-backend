# 初始化验证记录

日期：2026-09-16。环境：本机临时隔离 MySQL 8.4.11，未连接生产数据库。

- 按 migration-order.json 所列来源建立最终结构。
- 将最终结构与配置导出后，重新导入新的空数据库：74 张表、62 个菜单节点、171 条角色菜单关联。
- 用户、订单、支付、钱包和身份资料表均为 0 行。
- 管理员初始禁用，密码为不可登录占位值；无公开可用初始密码。
- Python 导入工具在另一个空数据库成功执行，验证了 74 张表。
- 管理员设置工具生成临时随机密码的 BCrypt 2a 散列，激活测试库管理员成功且密码校验通过；测试凭据不在本目录。

此记录不等于三端应用启动、现网迁移、支付或退款的验收。
