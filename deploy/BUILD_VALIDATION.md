# 构建交付验证 · 2026-09-16

本记录区分“源码可构建”与“新服务器业务验收”，不表示已在同事的服务器上线。

## 2026-09-20 商用前审查

补修后后端345项测试通过并生成可运行jar；实际MySQL/Redis应用启动与469项隔离HTTP检查通过，包含同一提现/售后重开并发、138条钱包历史分页；小程序158项、管理端36项及部署工具17项测试通过，三端编译成功。新空库导入75表成功。具体修复、数据库升级和未验证边界见 [商用审查报告](COMMERCIAL_AUDIT_20260920.md)。这不代表真实微信/Apple或生产历史数据已验收，也未部署线上；源码交付版本以Git提交记录为准，下方历史小节的远端一致仅指当时快照。

## 2026-09-19 支付修复增补

- 后端 JDK 8 / Maven 3.9.11 执行 `mvn clean package`，未跳过测试：276 项通过、0 失败、0 错误、0 跳过，成功生成可运行 jar。
- 固定 Surefire 3.2.5，使同事运行 `mvn test` 时能发现并执行 JUnit 5 测试。
- 新增 33 项回归测试覆盖回调配置与真实控制器路径、Apple 外部退款、漏通知补偿、已有/无售后记录、原流水和金额校验、沙箱隔离、重复及并发幂等、事务失败回滚、退款追偿与库存，以及第 101 笔核对不被前 100 笔失败永久阻塞。
- 支付查询用模拟微信响应，数据库业务回归用隔离 H2 MySQL 模式；未使用真实客户资料或密钥，未进行实际支付/退款。H2 回归不等于 MySQL、微信或 Apple 现网验收。
- 用户端重新运行 119 项测试及 UI 冻结检查全部通过；管理后台 4 项测试通过。本次两端源码未修改，远端与本地 main 已核对一致。
- 初始化结构及新增迁移都包含两列核对时间和两个索引。本次没有执行生产数据库升级；旧库应严格按 [支付部署与验收](PAYMENT_DEPLOYMENT.md) 先升级后更新程序。

## 环境与取材

Windows，Node.js 24.18.0 / npm 11.16.0，JDK 8 / Maven 3.9.11。使用独立仓库源码快照，在新的空目录安装依赖；未复用原项目 node_modules，未携带原 .env 文件。构建参数使用本机测试地址。

## 修复

- 管理后台录音工具收进自身 src/utils，移除跨项目目录引用，录音逻辑保持原样。
- 小程序 .npmrc 固定当前 UniApp 锁定依赖所需的 legacy-peer-deps 模式，普通 npm ci 可安装；未随意升级或降级依赖版本。
- 页面路由和 SVG 冻结检查只消除 CRLF/LF 差异，不修改视觉资源或冻结清单；实际内容变化仍检查失败。
- 业务检查不再读取已退役 mock 数据，检查真实支付与服务配置；修正已过期的支付状态文本断言。
- 管理后台 Vite 统一真实构建根路径，修复 Windows 目录映射到其他盘符时的 HTML 输出错误。
- 首次克隆需自行复制 .env.example，部署说明已明确。

## 验证结果

- 后端 clean package（跳过测试）：成功生成 ruoyi-admin.jar。
- 数据库：此前已在隔离 MySQL 8.4.11 空库重新导入 74 张表及初始化配置，详情见 [database/VALIDATION.md](../database/VALIDATION.md)。
- 管理后台：普通 npm ci、生产构建均成功，录音模块 3 项无真实麦克风测试通过。
- 小程序：普通 npm ci、微信小程序构建、H5 构建均成功。
- 小程序 119 项自动测试全部通过，路由、业务流程和冻结检查通过。
- Vue 模板、样式、图片、SVG 及 ui-freeze.manifest.json 未更改。

## 尚需由部署方完成

安装数据库、Redis、运行时和 Nginx；配置域名/HTTPS、私密环境变量及微信后台。
根据数据库说明激活自己的管理员账号。新空库需录入实际商品、客服和服务人员。
支付、退款、扫码登录、手机授权及同事服务器的端到端运行没有在本次构建验证中执行。
旧总仓库 sjz-club 是早期快照，最新交付以 sjz-club-backend / sjz-club-admin / sjz-club-app 三个独立仓库为准。
